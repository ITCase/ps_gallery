<!DOCTYPE html>
<html lang="${request.locale_name}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{{ gallery.name }}</title>

  </head>

  <body>

    <h1>Redefine me in "pyramid_sacrud_gallery/gallery.jinja2"</h1>

    <p>Breadcrumbs:</p>
    <ul>
      <li>
        <a href="{{ request.route_url('sacrud_gallery_list') }}">
          List of galleries
        </a>
      </li>
      <li> >> </li>
      <li>{{ gallery.name }}</li>
    </ul>

    <h2>Images from gallery "{{ gallery.name }}":</h2>
    <ul>
      {% for image in gallery.items %}
        <li>
          <a href="{{ request.route_url('sacrud_gallery_item_view', pk=gallery.get_val_pk(), image=image.get_val_pk()) }}">
            <img src="{{ image.path }}" alt="{{ image.description }}">
          </a>
        </li>
      {% endfor %}
    </ul>
  </body>
</html>
